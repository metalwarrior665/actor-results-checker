() => ({
    productName: (field) => typeof field === 'string' && field.length > 5,
    categories: (field) => Array.isArray(field) && field.length > 2 && typeof field[0] === 'string',
    description: (field) => typeof field === 'string' && field.length > 5,
    image1: (field) => typeof field === 'string' && field.startsWith('http'),
    price:  (field, item) => typeof field === 'number' || typeof item.estimatedPrice === 'number',
    estimatedPrice: (field, item) => typeof field === 'number' || typeof item.price === 'number',
    brand: (field) => typeof field === 'string',
    shippingPrice: (field) => typeof field === 'number',
    wiewsIn48Hours: (field) => field === undefined || typeof field === 'string',
    subCategory: typeof field === 'string',
    sellerCountry: typeof field === 'string',
    sellerState: typeof field === 'string',
    sellerCity: typeof field === 'string',
    sellerLocation: typeof field === 'string',
    sellerName: typeof field === 'string',
    sellerWatchesListed: typeof field === 'string',
    sellerReviewCount: typeof field === 'string',
    url: typeof field === 'string' && field.startsWith('https://www.chrono24.com/'),

    // dynamic fields
    "listingNumber": (field) => typeof field === 'string',
    "movement": (field) => typeof field === 'string',
    "caseMaterial": (field) => typeof field === 'string',
    "braceletMaterial": (field) => typeof field === 'string',
    "year": (field) => typeof field === 'string',
    "condition": (field) => typeof field === 'string',
    "scopeOfDelivery": (field) => typeof field === 'string',
    "gender": (field) => typeof field === 'string',,
    "movement/caliber": (field) => typeof field === 'string',
    "baseCaliber": (field) => typeof field === 'string',
    "powerReserve": (field) => typeof field === 'string',
    "numberOfJewels": (field) => typeof field === 'string',
    "caseDiameter": (field) => typeof field === 'string',
    "waterResistance": (field) => typeof field === 'string',
    "bezelMaterial": (field) => typeof field === 'string',
    "crystal": (field) => typeof field === 'string',
    "braceletColor": (field) => typeof field === 'string',
    "clasp": (field) => typeof field === 'string',
    "claspMaterial": (field) => typeof field === 'string',
    "dial": (field) => typeof field === 'string',
    "dialNumerals": (field) => typeof field === 'string',
    "code": (field) => typeof field === 'string',
    "braceletLength": (field) => typeof field === 'string',
    "lugWidth": (field) => typeof field === 'string',
    "thickness": (field) => typeof field === 'string',
    "frequency": (field) => typeof field === 'string',
    "buckleWidth": (field) => typeof field === 'string',
    "braceletThickness": (field) => typeof field === 'string',
    "model": (field) => typeof field === 'string',
    "referenceNumber": (field) => typeof field === 'string',
})
